---
name: testing-workflow
description: 当用户需要测试策略、回归方案、QA 工作流、验收清单、需求开发完成后的验证流程，或一套可复用的软件质量保障方法时使用。该 skill 定义了六步测试工作流：意图识别、影响面分析、风险建模、路径规划、精确执行、评估与知识沉淀。它也规定了何时应默认开启多智能体协作、为什么不应把 benchmark 当作主优化目标，以及为什么纯探索式测试不应作为默认策略。
---

# 测试工作流

这份 skill 用来把临场发挥式测试，变成可复用、可沉淀的质量工作流。

它不只是帮你“跑测试”，而是帮助你决定：

- 应该测什么
- 为什么要测
- 需要测到多深
- 应该走哪条验证路径
- 发现的问题怎样被记录和复用

这份全局 skill 只定义：

- 测试治理流程
- 决策规则
- 输出协议
- 多智能体分工
- 评估与沉淀方法

它不直接规定某个具体项目应该运行哪些测试命令、脚本或测试环境。
这些内容应由对应项目自己的 skill、仓库文档或项目约定来补充。

这份 skill 还基于 3 个方法论前提：

- 真实 bad case 是能力演进燃料，不只是失败记录
- 当前阶段应接受必要的工程补偿，而不是默认追求完全自治
- 真实业务成功率比 benchmark 分数更重要

## 先读哪些内容

先读：

1. `references/workflow.md`
2. `references/risk-model.md`
3. `references/anti-patterns.md`

再按需读取：

- 多智能体协作：`references/multi-agent.md`
- 决策表：`references/decision-tables.md`
- 示例：`references/examples.md`
- bad case 闭环：`references/bad-case-loop.md`
- 术语到执行动作映射：`references/term-mapping.md`
- 评估标准：`references/eval.md`
- 流程文件固定模板：`references/file-templates.md`
- 项目接入指南：`references/project-integration.md`
- 产物提交与保留策略：`references/artifact-retention.md`
- 输出格式与清单：`references/output-templates.md`

## 默认工作流

使用这 6 步：

1. 意图识别
2. 影响面分析
3. 风险建模
4. 路径规划
5. 精确执行
6. 评估与知识沉淀

除非用户请求非常窄且风险很低，否则不要一上来就直接跑命令。

## 项目级边界

如果用户正在某个具体仓库中工作，这份全局 skill 的职责是：

- 定义怎么思考和组织测试
- 定义如何输出计划与结果
- 定义何时开多智能体
- 定义如何沉淀 bad case

项目自己的职责是：

- 给出具体测试命令
- 给出测试环境选择规则
- 给出项目特有风险链路
- 给出项目特有断言与回归包

也就是说：

- 全局 skill 决定“怎么做测试治理”
- 项目 skill 决定“在这个项目里具体怎么测”

## 1. 意图识别

先判断这次测试的目标属于哪一类：

- 功能验收
- Bug 修复回归
- 状态或规则校验
- 集成链路确认
- 发布前信心检查
- 探索式辅助验证
- 环境或配置验证

最好先用一句话写出测试目标：

`我们需要确认 <行为> 仍然成立，并且 <具体故障> 不会再次回归。`

如果存在 PRD、需求单、交互稿、原型或验收标准，优先从这些材料提炼测试任务。
提炼时不要照抄文案，而是改写成可验证任务：

- 用户要完成的主动作
- 哪些角色各自应该成功
- 哪些状态必须变化
- 哪些失败分支必须拦住
- 哪些能力是当前阶段明确不支持的

如果 PRD 只有目标描述，没有完整步骤，也仍然可以先把“目标结果”提炼出来，再到代码和页面中补齐中间路径，但要明确哪些步骤来自推断。

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/01-intent.md`

## 2. 影响面分析

识别受影响面：

- 代码模块
- API
- 用户角色
- 状态迁移
- UI 面
- 数据依赖
- 运行环境

这一阶段的核心问题是：

`这次改动除了当前文件本身，还可能真实打坏什么？`

如果没有完整 PRD，不要放弃系统化测试。
应从下面几类事实源反推范围：

- 路由、控制器、API client、store
- 页面上的主按钮、主列表、主表单
- 历史 bad case、回归清单、测试产物
- 真实环境下已有行为

推荐顺序：

1. 找主入口
2. 找主动作
3. 找动作对应接口
4. 找接口对应状态变化
5. 找状态变化投影到哪些页面与角色

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/02-impact.md`

## 3. 风险建模

测试深度应由风险决定，而不是由方便程度决定。

建议分级：

- `P0`：资金、鉴权、权限、关键状态迁移、数据完整性、生产可用性
- `P1`：核心工作流、共享集成、多界面一致性
- `P2`：局部 UI 行为、孤立组件、低影响展示问题

风险越高，验证层次就应越多、覆盖面就应越广。

如果 PRD、代码、现网行为出现冲突，不要直接假设其中某一方天然正确。
先把冲突本身记录下来，再判断本轮测试暂时以谁为事实源：

- PRD 代表“期望行为”
- 代码代表“实现意图”
- 现网 / 测试环境代表“当前真实行为”

测试结论里必须写清：

- 三者分别是什么
- 当前按哪一个口径判定
- 这是文档过期、实现偏差，还是环境漂移

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/03-risk.md`

## 4. 路径规划

一份合格的测试路径规划应该回答：

- 哪条主链路必须通过
- 哪些失败分支或边界路径必须检查
- 适合用哪些自动化层
- 是否需要人工验证
- 是否需要远程环境或准生产环境

优先选择“最小但有意义”的验证集，而不是表面看起来很全的验证集。

典型顺序：

1. 本地快速检查
2. 模块测试或单元测试
3. 集成测试或 API 测试
4. 端到端或工作流测试
5. 必要时做真实环境验收

先走有边界、有引导的执行路径，再考虑开放式探索。

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/04-plan.md`

## 5. 精确执行

执行时必须边界清晰：

- 明确什么算成功
- 明确要收集哪些证据
- 明确什么时候停止

如果测试失败，至少记录：

- 失败了什么
- 失败发生在哪里
- 更像是代码、环境、数据还是预期漂移问题

除非任务明确要求，否则不要把执行过程变成无边界探索。

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/05-execution.md`

## 6. 评估与知识沉淀

每次正式测试都应以这些内容收尾：

- 测了什么
- 没测什么
- 覆盖了哪些风险
- 还剩哪些残余风险
- 学到了什么新的规则、边界或回归模式

同一类失败如果出现不止一次，就要沉淀成可复用的回归资产。

如果本轮存在 PRD、代码、现网冲突，评估阶段必须补一段“冲突收口”：

- 这次测试最后按什么口径给结论
- 哪个事实源需要被更新
- 是否阻塞发布或验收
- 是否需要新增一轮确认

本阶段输出应写入：

- `testing-artifacts/<task-id>/<round-id>/06-eval.md`
- `testing-artifacts/<task-id>/<round-id>/07-knowledge-capture.md`

## 输出目录规范

所有测试输出都应放在当前测试项目目录下：

```text
testing-artifacts/
  <task-id>/
    <round-id>/
      01-intent.md
      02-impact.md
      03-risk.md
      04-plan.md
      05-execution.md
      06-eval.md
      07-knowledge-capture.md
      planner-handoff.md
      executor-report.md
```

建议：

- `<task-id>` 使用稳定任务名，例如 `auth-regression`、`release-2026-05-14`
- `<round-id>` 使用轮次名，例如 `round-01`、`round-02`

每一轮只记录一次完整测试流程。
如果补测或重跑，新增新轮次，不覆盖旧轮次。

## 多智能体协作

当任务明显受益于并行或清晰分工时，应优先考虑多智能体协作。

默认分工：

- `Planner Agent`：负责意图、影响面、风险和路径规划
- `Executor Agent`：负责执行检查、收集结果、总结证据

不要让多个智能体重复做同一项验证。

满足以下任一条件时，若工具可用，应默认开启双智能体：

- 范围跨多个系统或多个验证层
- 一边分析一边执行能明显提速
- 用户明确要求双智能体或多智能体协作
- 需要一名智能体做规划，另一名智能体并行落测试据

以下场景不建议开多智能体：

- 任务很小且局部
- 下一步显而易见
- 协调成本大于实际工作量

### 双 Agent 标准交接字段

`Planner Agent` 交给 `Executor Agent` 时，至少要提供：

- 任务标识
- 轮次标识
- 测试目标
- 影响面摘要
- 风险等级
- 主链路
- 必测边界路径
- 推荐验证层
- 成功标准
- 停止条件
- 禁止偏离项
- 需要回传的证据类型

建议写入：

- `testing-artifacts/<task-id>/<round-id>/planner-handoff.md`

`Executor Agent` 回给 `Planner Agent` 时，至少要提供：

- 已执行项
- 未执行项
- 失败项
- 跳过项
- 原始证据位置
- 阻塞原因
- 初步归因
- 是否建议补轮次

建议写入：

- `testing-artifacts/<task-id>/<round-id>/executor-report.md`

详见 `references/multi-agent.md`。

## 关键原则

1. 优化真实信心，而不是优化“看起来覆盖很多”。
2. 优先围绕真实故障模式，而不是围绕好汇报的虚荣指标。
3. 把 benchmark 当基线和能力信号，不把它当主优化目标。
4. 把探索式测试当补充，不把它当默认主骨架。
5. 从真实 bad case 中提取可复用知识。
6. 把专业术语翻译成具体动作，不停留在抽象层。
7. 把输出写进项目目录，让测试工作可追踪、可比较、可回放。
